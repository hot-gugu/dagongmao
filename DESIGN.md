# 项目设计规范

## 文档用途

本文件用于长期沉淀本项目已确认、可复用的界面设计规范与设计决策，包括视觉风格、页面布局、组件样式、交互方式及资源管理要求。

## 维护原则

- 每轮涉及本项目界面调整前，必须先读取根目录 `BUSINESS_RULES.md` 与 `DESIGN.md`，按照两份文档的最新规则实施；完成后将本轮确认的长期业务规则与设计规则分别归并写回对应文档，避免实现与规范漂移。
- 仅记录已经确认且可长期复用的设计规则，不保存逐字对话、临时讨论或未确认建议。
- 新规则与既有规则重复时合并去重；明确变更时更新原规则，避免保留相互冲突的版本。
- 用户明确提出“记住”“以后统一”“所有页面都这样”等要求时，将其视为长期设计规则。
- 同时涉及设计和业务的规则，应在本文件与 `BUSINESS_RULES.md` 中分别记录，并在必要处相互引用。
- 后续对话中涉及视觉风格、布局、组件外观、交互表现、响应式适配和设计资源的已确认要求，统一整理到本文件。
- 从现有 HTML 或参考图中归纳出的内容，若用户尚未明确确认，只能标记为“当前实现基线”，不得擅自提升为长期设计规范。
- 每次更新应按主题归并到已有章节；除非确有新主题，不按对话轮次追加流水账。

## 文件与资源管理

- 当前项目根目录为：`D:\天泽智联\大工贸\数智大脑`。
- 当前页面工作稿为：`综合监管大屏.单位实时监测详情工作稿.html`。
- 当前视觉参考资源包括 `基础信息-v20.png`、`cctv-scenes.png` 及“场景一”至“场景十一”系列图片。
- 后续生成或修改的页面、脚本、样式、图片及规则文档统一保存在当前项目目录内。

## 当前实现基线（待后续确认与收敛）

- 整体产品是“合肥市工贸安全监管”监控大屏与业务工作台的组合形态。
- 大屏主体采用深色数据可视化风格，包含顶部主导航、三列态势布局、地图、指标、排行、预警、视频和处置进度等区域。
- 业务工作台及单位实时监测列表采用浅色后台风格，包含左侧分类/区域导航、指标区、筛选区、表格和详情抽屉。
- 当前 HTML 通过大量带版本号的内联 `<style>` 与 `<script>` 逐层覆盖旧实现。后续修改应优先识别最终生效规则，避免继续无边界叠加覆盖；需要重构时须保持现有业务入口和已确认交互不变。
- 当前设计存在深色大屏与浅色业务页两套界面语境。后续新增页面必须先判断所属语境，再复用对应页面的视觉语言，不能混用。

## 视觉与组件规范

- 当前单文件内的综合态势、风险预警、视频监管及其抽屉、区县画像和直播弹层统一采用已确认的深海军蓝政务主题：页面底色 `#071c38`，主卡片表面 `#0d2b54`，抬升表面 `#123866`，常规边框 `#2f5f93`，主交互高亮 `#5da8ff`，主文字 `#f4f8ff`，辅助文字 `#9db7d8`。顶部导航使用同色系横向渐变，内容页使用低对比径向渐变增强层次；不得再让不同一级页面各自使用相互冲突的青绿或灰黑主题。
- 统一主题只替换品牌与表面颜色，不改变风险语义：红色紧急、橙色较大、蓝色提示、绿色成功等既有等级和状态色继续保留，避免用统一主题蓝覆盖业务语义。
- 新增或调整页面时，优先复用系统已有页面、组件和视觉语言。
- 同类按钮应统一尺寸、颜色、边框、圆角、文字与悬停状态。
- 同类抽屉应统一外框、标题区、关闭方式、宽度规则和内容间距。
- 同类表格应统一表头、行高、边框、状态展示和操作栏样式。
- 分页组件应保持统一的页码、上一页、下一页及禁用状态风格。

## 交互规范

- 新增交互不得破坏既有页面加载、路由切换和其他组件行为。
- 页面增强功能应避免循环监听、重复绑定和持续重绘。
- 修改完成后应验证页面可正常加载，主要操作可用，且样式与系统现有页面一致。

## 综合态势地图

- 当前单文件大屏顶部仅保留“综合态势、风险预警、视频监管”三个一级菜单；“警情中心”作为独立 HTML 页面维护，不得在本文件中恢复其菜单、页面克隆、路由、重定向入口或右上角“主界面”返回按钮。
- 视频监管九宫格使用真实工业监控场景作为画面底图；每个窗口底部固定显示企业、摄像头编号和最大化播放入口，点击最大化后以当前画面进入直播预览，全屏失败时保留页面内大画面兜底。
- 视频监管实时预警列表的时间位于行首，风险等级固定在该行最右侧，部位和企业信息在下方分层展示。
- 综合态势地图只保留行政区底图轮廓与区县名称。
- 不展示地图底部的图层切换 Tab；原“企业类型、风险等级、报警热力、设备运行”等入口整体移除。
- 不展示企业类型图例、报警图例或其他撒点图例。
- 地图上不展示企业、设备、报警、风险、热力等任何撒点或点位高亮信息。
- 地图首次进入和点击复位时均按行政区轮廓等比最大化，完整显示、不拉伸、不裁切，上下保留约 20–24px 安全边距。
- 每个区县使用双层标签：第一行区县名称，第二行“重点部位 XX处”；数字使用数字字体和青蓝高亮，语义文字使用辅助色。
- 点击区县后仅高亮当前行政区轮廓，不弱化或隐藏其他区县。
- 区县详情从页面右侧以约 600px 宽抽屉滑入，使用低透明遮罩保留地图上下文；抽屉内容独立滚动，关闭后恢复全市地图状态。
- 区县详情不新增内部页签，监管底数、风险管控、监测预警、处置闭环四个板块纵向排列。
- 区县监管画像与其他右侧宽抽屉保持统一字号层级：抽屉标题 `20px`、副标题 `13px`、板块标题 `16px`、正文及指标标签 `12px`、主要数值 `14px` 或 `22–24px`；图表刻度不小于 `10px`，图例不小于 `11px`。抽屉内部不得继续使用 `8–9px` 的可见业务文字。
- 综合态势左侧悬浮区固定为两张纵向卡片，监管底数与风险管控高度约为 `40% / 60%`；监管底数必须稳定完整显示四类企业数据。风险管控的重点部位列表在常规大屏下仅保留约五条可视高度，其余类别在该区域内部滚动；矮屏进一步压缩分类区与区县列表高度，避免卡片内容被整体裁切。
- 监管底数采用三项可点击指标与四类企业接入表格；表头依次为企业类型、企业接入（已接入/应接入）、接入率、设备数。每类企业只使用一根接入完成度进度条，进度条与“已接入/应接入”数字在同一行横向排列；接入率与设备数作为独立数值列展示，不与企业数共用比例尺。
- 风险管控的九类重点部位占比条采用单列逐行展示，每条内部固定为左右双列：左侧名称列固定 `58px`，与监管底数的企业类型列同宽；列间距统一为 `5px`，保证两张卡片的进度条从同一竖线开始。长名称允许换为两行且不使用省略号；右侧进度条高度与监管底数统一为 `7px`，并随卡片可用宽度自适应伸缩。“数量 · 占比”固定在整行最右侧且右边缘对齐，不跟随进度条长度向左漂移。左右两列始终处于同一条目中，不得因名称换行导致进度条掉到下一行。下方列表使用较紧凑的固定高度，不设置顶部横向分割线，标题统一为“各区县较大风险部位情况”，悬停时暂停自动滚动。
- 左侧两张卡片除主标题外采用三档字号：业务名称与主要数据使用 `12px`，表头使用 `10px`，辅助说明使用 `11px`；核心指标大数字继续沿用既有数字字号，不随正文放大。
- 企业列表和重点部位列表统一使用右侧宽抽屉，信息层级固定为“类别汇总统计卡—查询筛选条—明细表格”。统计卡使用六列紧凑布局，筛选条包含企业名称、行政区域、企业类型、可选接入状态、重置和筛选结果；统计卡保持当前下钻类别的全量口径，筛选只更新明细及结果条数。
- 宽抽屉标题使用 `20px`，副标题、筛选控件和表格正文统一不小于 `13px`，数字单元格可提高至 `14px`。表格优先使用百分比列宽铺满抽屉，企业名称保留主要空间，短字段压缩、长表头允许两行、超长内容省略；常规大屏仅纵向滚动，窄屏才启用横向滚动兜底。重点部位抽屉不重复展示已由抽屉标题明确的“重点部位名称”列，企业名称直接作为详情下钻入口。企业、重点部位、区县画像和事件详情抽屉互斥，同一时刻只显示一个。
- 综合态势主内容区采用“一张全宽地图底板 + 左右业务卡片悬浮层”，地图不再受中央卡片边框、圆角和三列容器限制。
- 左侧使用监管底数、风险管控两张卡片；右侧使用“监测警情清 / 处置闭环清”两张卡片，并按约 `46% / 54%` 分配高度。卡片统一使用深色半透明表面、细边框、轻阴影和适度背景模糊悬浮在地图上方。
- “监测警情清”采用四项可点击指标和近30天三等级折线图；折线保留每日数据点，仅稀疏显示日期刻度。“处置闭环清”采用四项可点击指标和区县处置滚动列表；区县列表悬停暂停、支持滚轮浏览。
- 警情指标、折线数据点、闭环指标和区县行均从右侧打开统一宽事件抽屉。抽屉沿用“汇总统计卡—查询筛选条—明细表格”层级，正文不小于 `12px`，企业名称作为详情下钻入口；常规宽度采用百分比列宽铺满，窄屏允许横向滚动兜底。
- 地图标题与筛选工具条作为顶部悬浮层，仅占左右卡片之间的区域；地图控件贴近右侧卡片内侧放置。
- 地图工具区不渲染企业名称搜索框；仅保留区县下拉（默认"全市"）与恢复全市按钮，下拉样式与顶部筛选控件一致。
- 左侧风险管控卡片内部的重点部位滚动列表初始化时停留在顶部，不得自动滚到底部；后续自动滚动只在用户未悬停时从顶部向下循环。
- 地图支持以鼠标位置为锚点的滚轮缩放：向上放大、向下缩小；缩放不得影响悬浮卡片、列表和区县详情抽屉自身的滚动。
- 地图保留拖拽平移和“放大 / 缩小 / 复位”按钮；复位恢复完整全市视图，拖拽结束不得误触发区县详情。
- 地图最小缩放为完整全市适配比例，最大缩放为默认比例的 4 倍；不得无限缩放或让地图消失。
- 综合态势的地图底板渐变应延伸到 `body` 主体并随视口自适应通铺，页面四周不得出现与内容区割裂的背景色块；地图舞台自身保持透明承接全页底板。
- 点击风险预警趋势曲线点后，事件列表统一从页面右侧以抽屉形式打开；抽屉使用深色大屏视觉、独立滚动和固定表头，避免居中弹窗遮挡地图主体。

## 风险预警地图

- 风险预警地图沿用深色工业化政务监管大屏视觉，地图底板铺满主体并作为页面背景，不引入浅色业务后台表面。
- 风险预警地图使用本地离线合肥市基础地理底图，包含道路、铁路、水系、道路名称及行政边界；底图与企业、警情、设备点位统一使用 WGS84 经纬度投影。全市视角弱化支路和道路名称，放大后逐步呈现真实道路网格与名称。
- 离线基础地理图层在合肥市边界外使用半透明深色遮罩，保留弱化后的外部路网作为空间参照，但不得出现彩色线框。全市视角下所有道路统一置灰；进入区县后，区外路网继续以低对比灰色保留，当前区县内叠加清晰的矢量道路和道路名称，业务点位仍严格限定在当前区县。
- 区县视图以矢量道路为主体，静态总览底图降低至弱辅助层；区县内支持最高 `12×` 放大，道路名称、次干道和支路按缩放层级逐步出现。点击企业、警情或设备点位时先以其经纬度居中并放大到道路级，再打开对应详情。
- 离线底图资源独立于主 HTML 保存，页面不得在运行时请求在线地图服务；底图缺失时保留行政区轮廓并显示明确的资源缺失状态，不生成虚构道路。
- 风险预警页恢复左右两张整栏悬浮卡，左侧为“风险实时监测”，右侧为“警情风险研判”；原有左右栏 DOM、数据和脚本继续保留，不删除旧业务代码。
- 风险预警中央地图不使用卡片边框、圆角、阴影或独立背景限制；筛选、图层、图例和缩放控件作为悬浮工具层直接覆盖在全幅地图上。
- 风险预警地图底部不展示“离线道路底图、坐标系、放大查看道路名称”等面向研发的技术说明，避免与业务图层控制争夺视觉注意力。
- 图层控制位于左右研判栏之间的地图底部，采用横向五项 Tab：企业点位、实时警情点位、风险等级、报警热力图、设备运行。五项均为独立多选开关，默认仅开启企业点位；选中态必须清晰，支持多个专题同时叠加。动态图例位于图层控制左上方并采用竖向排列，只展示当前已开启图层的对应图例。
- 地图顶部工具区提供行政区和四类企业类型筛选；筛选结果数量、当前图层数量与地图点位同步更新。
- 企业类型使用四种同系列 Iconfont 语义的本地内嵌 SVG 图标，分别表达危化工厂、金属熔融、矿山和有限空间；图标随地图缩放在限定范围内平滑改变屏幕尺寸。风险等级作为企业图标外圈叠加。高风险/紧急使用 `#ff6673`，较大/中风险使用 `#ffc076`，一般/低风险使用 `#6199ff`。
- 企业点位统一采用蓝紫色圆形“企业”SVG 标记，右上角使用 `V / 熔 / 矿 / 限 / 粉` 短标签区分工贸涉危、金属熔融、非煤矿山、有限空间和粉尘涉爆；风险等级继续由标记外圈表达。实时警情使用区别于企业点位的立体警示三角：高风险为红色、较大风险为橙色、一般风险为蓝色，并使用与等级颜色一致的两层错峰扩散光圈；动画遵守减少动画偏好。点击警情点打开对应事件详情，同坐标聚合时显示数量。
- 全市视图按行政区聚合，中等缩放级别按屏幕网格聚合，高缩放级别展示具体企业、警情和设备点位；聚合标记必须显示数量并可点击下钻。
- 设备明细点位采用附件参考的“水滴形地图定位针 + 内嵌设备屏幕与支架”矢量造型；在线使用青绿色，离线使用低饱和警示橙灰色，图标底部保留克制光晕，并与企业、警情图标保持明显区分。
- 报警热力图采用低、中、高三级连续辐射色，透明度不得遮蔽行政区边界、名称和前景点位；热力图可与其他图层同时显示。
- 点位悬停显示简要信息，点击后统一从右侧打开企业档案、事件详情或设备详情抽屉，保持地图上下文；点位与聚合标记均须支持键盘聚焦。
- 风险预警企业安全档案抽屉必须沿用深色监管大屏表面（深蓝底、青蓝强调、低对比边框）；底部操作栏与“调阅企业视频”按钮不得回落为浅色后台样式。
- 地图复位只恢复空间视角，不能清空图层与筛选；空白点击只关闭临时提示，不触发筛选重置。
- 点击区县边界、区县名称或全市级区县聚合点后进入真实区县下钻视图，其他区县边界、标签及业务点位全部隐藏；区县内部绘制提供的真实街道/乡镇 GeoJSON，不采用整图放大或模拟网格。
- 区县下钻顶部显示“全市 / 当前区县”面包屑和“返回全市”操作。区县内复位保持当前区县并恢复最佳视角；没有街道边界数据时在地图中央显示克制的待接入空态。
- 左右研判卡采用深色半透明整栏表面并直接悬浮在全幅地图上，不在内部继续套用小卡片。常规大屏左栏约 `320px`、右栏约 `340px`，矮屏可分别收敛至约 `300px / 320px`；地图标题、筛选、图层、图例和缩放控件必须位于两栏之间。
- 左栏内部按约 34% / 27% / 39% 分为实时警情跟进、重点部位警情、较大及以上事件跟进；右栏内部按约 24% / 22% / 27% / 27% 分为高频事件类别、报警时间段、高频企业 Top10、高频部位 Top10。
- 左右栏分别使用紧凑时间下拉；环状图仅用于等级事件占比，重点部位与排行使用横向占比条，最近 24 小时事件列表自动滚动并在悬停、聚焦或减少动画偏好下暂停。
- 最近 24 小时事件列表按约 `80–90s` 完成一轮滚动，当前演示数据使用 `86s`，避免快速滚动影响阅读。

## 关联文档

- 业务场景和业务规则见 [BUSINESS_RULES.md](./BUSINESS_RULES.md)。
